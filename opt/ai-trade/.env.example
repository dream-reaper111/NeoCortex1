# Copy this file to .env and fill in secrets before deploying.
# Redis connection string used by FastAPI workers and trainer.
REDIS_URL=redis://:change-me@redis:6379/0

# Internal PostgreSQL/TimescaleDB URI for application services.
DB_URL=postgresql://ai_trader:${DB_PASS}@db:5432/ai_trading

# Database superuser password (keep in sync with DB_URL placeholder above).
DB_PASS=replace-with-strong-password

# JWT signing key for API access tokens.
JWT_SECRET=generate-a-long-random-string

# Authelia JWT secret for session validation.
AUTHELIA_JWT_SECRET=generate-another-long-secret

# Ngrok authentication token from your ngrok account dashboard.
NGROK_AUTH_TOKEN=replace-with-ngrok-token

# Optional values:
# AUTHELIA_SESSION_SECRET=optional-session-secret
# GRAFANA_ADMIN_PASS=strong-grafana-password
# PUBLIC_DOMAIN=example.com
