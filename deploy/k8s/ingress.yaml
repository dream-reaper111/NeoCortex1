apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: neocortex-api
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: neocortex.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: neocortex-api
                port:
                  number: 80
